{% extends 'home/template.back.html.twig' %}

{% block title %}New Section{% endblock %}

{% block body %}
    <h1>Create new Section</h1>

    {{ include('admin_section/_form.html.twig') }}

    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{% endblock %}
